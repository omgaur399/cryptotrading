"# Task List - Drawing Primitives Fixes\n\n- [ ] Implement `intervalToSeconds` and `DrawingBase.timeToX` in `drawing-primitives.js`\n- [ ] Update `DrawingBase._getCoords()` and `DrawingBase.getHandles()` to use `timeToX`\n- [ ] Update `LongPositionRenderer.draw()` and `ShortPositionRenderer.draw()` to use `timeToX`\n- [ ] Implement `getTimeFromParam` snapping logic in `script.js`\n- [ ] Update click and hover event logic in `script.js` to support blank space drawing\n- [ ] Update `_screenToChart` coordinate conversion in `script.js` to support future space dragging\n- [ ] Update `DrawingManager.bindDragHandles` to prevent double-binding and call it in `initializeChart`\n- [ ] Implement primitive serialization (`savePrimitiveDrawing`) and attach it to finalization and drag events\n- [ ] Add `restorePrimitive` function and call it in `restoreDrawings`\n- [ ] Call `drawingManager.clearDrawings` inside `resetChart`\n- [ ] Add primitive deletion support via the hover delete button\n- [ ] Manually verify all drawing, dragging, and persistence functionality\n"